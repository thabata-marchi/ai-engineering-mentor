# dependências
node_modules/

# segredos e ambiente (NUNCA versionar) — use .env.example como modelo
.env
.env.*
!.env.example

# sobras do Python (o projeto migrou para TypeScript)
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# ferramentas / editor / SO
.qodo/
.vscode/*
!.vscode/mcp.json
.idea/
.DS_Store

# build (JS compilado p/ npm — reconstruível com `npm run build`; não versionar)
dist/

# dados gerados (o índice de vetores é reconstruível — não versionar)
data/vectorstore/

# materiais de estudo pessoais (PDFs/livros com direitos autorais) — NÃO versionar
data/
